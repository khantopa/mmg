import { css } from '@emotion/css';

export const containerStyles = css({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
});

export const joinFormStyles = css({
  maxWidth: 420,
  width: '100%',
  // margin: '40px auto ',
  padding: 16,
});

export const joinFormTitleStyles = css({
  textAlign: 'center',
});
